#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 * @p: Will have an number to be computed
 * Return: the absolute value of number or zero
 */
int _abs(int p)
{
if (p >= 0)
{
return (p);
}
return (-p);
}
